import {DocsImage} from '../../../components/docs/Image';
import dimg from "../../../lib/docsImages";

# Oracle Cloud

<div className="flex justify-center my-6">
  <DocsImage src={dimg.logo.oracle} alt="Cloudflare" border={false} style={{maxWidth: 300}}/>
</div>


원래 Render 에서 배포하려다가 여러 이유로 전향한거 적기