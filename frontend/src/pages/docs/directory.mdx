import RenderTreeComponent from "../../components/docs/RenderTree";
import {Tabs} from 'nextra/components'

# 프로젝트 디렉토리 구조

<Tabs items={['디렉토리 구조', '디렉토리 구조 (파일 포함)']}>
  <Tabs.Tab>


```
/KIN-Web
    ├─ /backend
    │   ├─ /config              # 각종 환경 설정
    │   ├─ /controllers         # API 요청 처리
    │   │   ├─ /notes
    │   │   └─ /user
    │   ├─ /middleware          # 세션 유효성 검사 및 로깅, 에러 핸들러
    │   │   └─ /user
    │   ├─ /models              # 데이터베이스 모델
    │   ├─ /routes              # 라우트 정의
    │   │   ├─ /notes
    │   │   └─ /user
    │   ├─ /services            # 비즈니스 로직
    │   │   ├─ /admin
    │   │   ├─ /notes
    │   │   └─ /user
    │   └─ /utils               # 유틸리티 함수
    └─ /frontend
        ├─ /public              # 정적 리소스 파일
        │   ├─ /fonts
        │   └─ /images
        │       ├─ /demo
        │       ├─ /docs
        │       └─ /loginlogo
        └─ /src
            ├─ /atoms           # 전역 상태 관리
            ├─ /components      # 재사용 가능한 컴포넌트
            │   ├─ /admin
            │   ├─ /auth
            │   ├─ /docs
            │   ├─ /introduce
            │   ├─ /notes
            │   │   └─ /editor
            │   │       └─ /plugins
            │   ├─ /plate-ui
            │   ├─ /ui
            │   └─ /userinfo
            ├─ /hooks
            ├─ /lib
            │   ├─ /hoc          # 고차 컴포넌트
            │   └─ /notes
            ├─ /pages            # 웹 페이지 구성
            │   ├─ /_authentication
            │   ├─ /admin
            │   ├─ /api
            │   │   └─ /proxy
            │   ├─ /docs
            │   │   ├─ /auth
            │   │   ├─ /details
            │   │   ├─ /etc
            │   │   ├─ /issues
            │   │   └─ /notes
            │   ├─ /notes
            │   └─ /userinfo
            ├─ /services         # API 호출 및 서비스 로직
            │   ├─ /notes
            │   └─ /user
            └─ /styles           # 글로벌 스타일 파일
```

  </Tabs.Tab>
  <Tabs.Tab>
<div className="flex my-5 justify-center grid-cols-3 gap-3">
  <RenderTreeComponent/>
</div>
<div className="flex justify-center">
  파일을 누르면 해당 파일의 github 로 이동합니다.
</div>
  </Tabs.Tab>
</Tabs>